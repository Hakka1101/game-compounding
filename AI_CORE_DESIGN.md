# AI用基本設計：調合システム

## 1. システムの核
本システムは「未知の法則の解明」をテーマとする。
プレイヤーは「素材のメモ」と「レシピのヒント」を頼りに試行錯誤し、一度成功（または特定の条件をクリア）することで「正式なレシピ」を解禁する。

## 2. データの循環
1. **探索（将来実装）**: 素材を獲得し `ItemDictionary` を更新（未発見 -> 発見済）。
2. **調合**: 手持ち素材と工程を組み合わせる。
3. **判定**: `Recipes` データと照合。
4. **解禁**: 成功時、`PlayerStatus.unlockedRecipes` にIDを記録。これにより「代替素材」や「正確な手順」がUI上で可視化される。

## 3. レシピの拡張性（代替素材）
レシピは「特定のアイテムID」だけでなく「カテゴリー（例：薬草類）」や「属性（例：火属性）」での指定を可能とし、レシピ解禁後はそれら代替可能な候補がシステム上で明示される。