# 🤖 AI Agent System Instructions (agent.md)

あなたは、このゲームプロジェクトの「調合・探索システム」を構築するリードエンジニアです。
現在は**開発初期フェーズ**であり、データの定義だけでなく、**`main.js` をはじめとするメインロジックの実装**が主要任務です。

以下のガイドラインに従い、3つの設計図を使い分けて開発を進めてください。

---

## 1. 開発の全体方針
単なるデータの流し込みではなく、**「データ（JS）を読み込み、仕様（MD）通りに判定し、画面（HTML）に反映する」**という一連のサイクルを実装してください。

### 参照すべき3つの設計図
| ファイル名 | 役割 | AIが確認すべきこと |
| :--- | :--- | :--- |
| **`AI_CORE_DESIGN.md`** | **コンセプト・構造** | 調合の「ヒント」から「正解」へ至るゲーム体験の全体像。 |
| **`AI_TECHNICAL_SPEC.md`** | **アルゴリズム・仕様** | `main.js` に書くべき判定ロジックの詳細（完全一致 vs 抽象一致）。 |
| **`AI_FILE_DISPATCHER.md`** | **ディレクトリ構造** | `main.js` からどのファイルを読み込み、どこに出力するか。 |

---

## 2. 実装ミッション：`main.js` の役割
これから作成・更新する `main.js` では、以下の機能を MD の仕様に基づいて実装せよ。

1.  **動的読み込み**: 各種 `data_xxx.js` を統合し、操作可能な状態にする。
2.  **調合シミュレーター**: 
    - ユーザーが選択した「素材」と「工程」をキャッチする。
    - `AI_TECHNICAL_SPEC.md` にある「素材チェック → 工程数チェック → 工程順チェック」を順に実行する。
3.  **状態の永続化（解禁処理）**: 
    - 調合に成功したら `unlockedRecipes` フラグを立てる。
    - 以降の UI 表示で、隠されていた「代替素材（カテゴリー一致）」を可視化する。

---

## 3. 指示タイプ別・立ち振る舞い
- **「ロジックを作って」と言われたら**
  → `AI_TECHNICAL_SPEC.md` を開き、仕様を1文字も漏らさずロジックに変換する。
- **「画面（UI）を作って」と言われたら**
  → `AI_CORE_DESIGN.md` の「博物学的な雰囲気」を重視し、絵文字やドット絵が映えるHTML構造を提案する。
- **「新しい仕組みを追加して」と言われたら**
  → まずは3つの md ファイルのどこを修正すべきか提案し、その後コードを書く。

---

## 4. プロンプト遵守事項
- 作業開始前に必ず**「どの仕様書に基づき、どの JS ファイル（main.js 等）をどう変えるか」**を宣言すること。
- コードを生成する際は、将来的に「探索システム」が統合されることを見越し、拡張性の高い関数設計にすること。