# AI用操作ガイド：ファイル構成と役割

依頼内容に応じて、以下のファイルを優先的に読み込み・編集すること。

## 1. データ定義（静的データ）
- `data_items.js`: アイテムの基礎定義（ID, 名前, 属性, ヒント）。
- `data_recipes.js`: 正解の組み合わせ、工程、解禁時の情報。
- `data_processes.js`: 4元素（火水土風）と工程の対応表。

## 2. 状態管理（動的データ）
- `player_state.js`: プレイヤーの所持品、解禁済レシピ、図鑑の解禁状況。

## 3. ロジック・UI（プログラム）
- `logic_mixer.js`: 調合の判定アルゴリズム本体。
- `ui_manager.js`: 錬金術所の表示、図鑑の更新、レシピ一覧のレンダリング。

---

## 逆引き辞書（AI用）
- **「新しい素材を追加して」** -> `data_items.js` を編集
- **「調合に失敗した時の挙動を変えて」** -> `logic_mixer.js` を編集
- **「レシピが解禁された時のUI演出を強化して」** -> `ui_manager.js` を編集
- **「特定の素材で代用できるようにして」** -> `data_recipes.js` の `ingredients` 構造を編集