# AI用実装仕様：調合・解禁ロジック

## 1. 判定アルゴリズム
調合実行時、以下の優先順位でレシピを走査する。
1. **完全一致レシピ**: 素材ID、工程、順序がすべて一致。
2. **抽象一致レシピ**: カテゴリー/属性条件を満たし、工程が一致。
3. **失敗作**: 上記いずれにも該当しない場合、投入素材の属性から「失敗作（汚物、毒など）」を生成。

## 2. レシピ解禁システム
- `unlockedRecipes`: プレイヤーが所持している解禁済レシピIDの配列。
- **解禁の効果**: 
    - 錬金術所のUIにレシピが表示される。
    - レシピ内の `ingredients` が「カテゴリー指定」の場合、所持している該当素材がリストアップされる。

## 3. 図鑑（Encyclopedia）管理
- 素材を一度でも入手、または調合で作成した場合、`ItemDictionary` の `isKnown` フラグを `true` に書き換える。
- `isKnown` が `false` のアイテムは、UI上では「？？？」や「ヒントテキスト」のみ表示する。